[build]
    command = "make -C themes/gcc && hugo"
    publish = "public"

[build.environment]
    HUGO_VERSION = "0.111.3"

[context.production.environment]
    HUGO_ENV = "production"

[context.deploy-preview]
    command = "make -C themes/gcc && hugo --buildDrafts"
